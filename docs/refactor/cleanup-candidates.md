# Cleanup candidates and preservation review

This is an evidence log, not a deletion list. Phase 2 makes no cleanup change.
Confidence describes confidence in the classification, not permission to
remove an item.

## Candidate items

| Item | Classification | Confidence | Evidence | Required review before removal |
| --- | --- | --- | --- | --- |
| `.claude/commands/*` | `development-only` / `candidate-dead` | medium | No browser/backend import; command files refer to verification or drafting workflows by string/path. | Check agent tooling discovery and command invocations. |
| `.claude/skills/medsim-interview.md`, `medsim-patient-generator.md`, `medsim-triage-logic.md` | `development-only` / `documentation-archived` | medium | Prompt/authoring artifacts; historical triage language appears in agent documentation, not the production route graph. | Check authoring workflows and graduation-project records. |
| `loop/babysit-simulation.md`, `loop/keep-thinking.md`, `scripts/loop/verify-loop.ts` | `development-only` | low | Loop automation is not imported by the app; tests verify command/runner relationships. | Inspect scheduled/local automation and `.claude` references. |
| `backend/local_ai_benchmark.py`, `backend/local_ai_preflight.py`, `backend/openrouter_preflight.py` | `development-only` | low | Standalone `__main__` tools and PowerShell consumers; no normal server import path. | Preserve if deployment/preflight documentation still promises them. |
| `backend/smoke_test.py`, `backend/tts_benchmark.py` | `development-only` | low | Standalone smoke/benchmark entry points; TTS benchmark directly imports the provider package. | Check manual QA and release scripts. |
| `scripts/download-ranged.ps1`, `setup-local-ai.ps1`, `start-local-ai.ps1`, `start-medsim-local.ps1`, `stop-medsim-local.ps1` | `development-only` | low | External PowerShell entry points create local runtime/model processes and artifacts. | Check operator workflows; never infer deadness from TS imports. |
| `docs/archive/evolution.md`, `docs/legacy-case-migration.md`, `docs/archive/local-ai-migration-report.md` | `documentation-archived` | high | Explicitly historical/migration material; current analysis and export scripts reference portions of it. | Remove only with link/reference audit. |
| `docs/generated/medsim-medical-cases-review.xlsx` | `generated` | low | Review deliverable generated by clinical export code; no runtime import found. | Verify academic/review handoff requirements. |
| `docs/generated/medsim-medical-cases-review.json` | `generated` | low | Review export artifact; source provenance references are consumed by review tooling, not runtime. | Verify export reproducibility and archival policy. |

## Items that are not dead despite weak or indirect evidence

- `src/components/AgenticRoundsScreen.tsx` and
  `src/components/AgentTopologyScreen.tsx` are directly imported by `App.tsx`
  and have screen-state branches.
- `src/data/defaultTestResults.ts` is consumed by tests and clinical review
  export tooling and preserves explicit unavailable-result behavior.
- `src/data/autoRubric.ts` is imported by `debriefRequest.ts` and verification
  scripts; it remains a fallback contract for cases without authored rubrics.
- `src/clinical/migration.ts` is consumed by clinical audit and review export.
- `backend/migrations/001_auth.sql` is discovered by a filename glob at runtime.
- `docs/generated/curation-manifest.json`,
  `investigation-manifest.server.json`, and
  `local-ai-manifest.server.json` are loaded by the backend and/or security
  tests; generated does not mean disposable.
- `public/medsim.mp3` is loaded through the literal `/medsim.mp3` URL.
- `middleware.ts`, `vercel.json`, `backend/Procfile`, and PowerShell scripts are
  deployment or operator entry points and are not statically imported by React.

## Legacy artifact findings

- No production Emergency Room route, dataset, mode, or triage screen was
  found. Outpatient red flags and urgent escalation remain in current cases
  and must not be mistaken for a legacy ER feature.
- MedKit/hackathon terminology appears in prompts, historical documentation,
  generated provenance, or tests; no standalone production MedKit module was
  identified.
- Former cloud/Anthropic voice or model references are historical or negative
  security assertions. Current runtime paths are provider-neutral/OpenRouter
  plus local TTS, and tests explicitly guard removed providers.
- `src/clinical/migration.ts` and `docs/legacy-case-migration.md` describe the
  168 archived-case decision. They are preservation-critical evidence, not
  deletion candidates.

## No high-confidence production deletion candidates

Static analysis found no active-runtime file with high-confidence dead status.
Every item listed above is development-only, historical, generated, or
externally invoked and requires an explicit later cleanup phase with consumer
checks. Do not begin dead-code deletion from this inventory.
