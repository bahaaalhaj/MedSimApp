# Local patient speech. Prepare assets explicitly with backend/prepare_kokoro.py.
kokoro>=0.9.4,<0.10
soundfile>=0.13,<0.14

# Optional and intentionally not installed by default:
# chatterbox-tts==0.1.7
